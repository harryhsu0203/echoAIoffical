services:
  - type: web
    name: pivot-ai
    runtime: static
    buildCommand: "cd pivot-app && npm install && npm run build"
    staticPublishPath: "pivot-app/out"
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
